// Remove characters
// You’re checking over a pile of application forms containing confidential data. You want to redact sensitive information first before you forward these in an email by removing first and last letters of a name (string). Write a function that removes the first and last letters of a name(string). Assume name is not less than 2 letters.

function removeLetter() {}

module.exports = {
  removeLetter
};
